Challenge:

Have a button to scramble the list. Display the result.
Have a button to apply Bubble Sort (O(n^2)) to the list (sort by last name). Count and display the number of passes it took.

Have a button to apply Merge Sort (O(nlogn) to the list (sort by last name). Count and display the number of passes it took.